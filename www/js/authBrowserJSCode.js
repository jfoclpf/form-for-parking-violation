/* tenta lidar sozinho com a página da Autenticação autenticação.gov
https://cmd.autenticacao.gov.pt/Ama.Authentication.Frontend/Processes/DigitalSignature/DigitalSignatureIntro.aspx */

(function () {
  // primeiro passo, há o botão "Avançar" e não há o botão "Cancelar"
  // Clica no botão "Avançar"
  /* var buttonNext = document.getElementById('MainContent_btnNext')
  var buttonCancel = document.getElementById('MainContent_btnCancel')

  if (buttonNext && !buttonCancel) {
    buttonNext.click()
  }

  // esconde o botão "Cancelar" quando está presente
  if (buttonCancel) {
    buttonCancel.style.display = 'none'
  }

  // esconde o botão "Voltar" quando está presente
  var buttonBack = document.getElementById('MainContent_btnBackReg')
  if (buttonBack) {
    buttonBack.style.display = 'none'
  }

  // carrega no botão Sim na parte de "Tem a certeza que é este o documento a assinar?"
  var textEl = document.getElementById('MainContent_lblTitlePreviewDoc')
  var buttonYes = document.getElementById('MainContent_btnYes')
  var buttonNo = document.getElementById('MainContent_btnYes')
  if (textEl && textEl.innerText.indexOf('documento a assinar') !== -1 && buttonYes && buttonNo) {
    buttonYes.click()
  }

  // em "Detalhes da assinatura do documento" clica "Submeter"
  textEl = document.getElementById('MainContent_lblSignatureDetailsTitle')
  var submitButton = document.getElementById('MainContent_btnSignDetails')
  if (textEl && textEl.innerText.indexOf('assinatura do documento') !== -1 && submitButton) {
    submitButton.click()
  } */
})()
